<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.centero.ghg.client.lang.mapper.LangMapper">

    <!-- 사용자 Role의 메뉴 목록 조회 -->
    <select id="findLangByCond"  resultType="Lang">
        /* SQL_ID : kr.centero.ghg.client.lang.mapper.LangMapper.findMenuByCond */
        /* 메뉴 목록 조회 */
        SELECT
            LOCALE_CD,
            LANG_CD,
            DSP_TEXT
        FROM TB_CMM_LANG
        WHERE LOCALE_CD = #{locale}
        ORDER BY LANG_CD ASC
    </select>


</mapper>
